Set-Content -Path "pyproject.toml" -Value '[project]
name = "qr-pet-backend"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
"fastapi>=0.117.1",
]

[tool.vercel]
entrypoint = "main:app"'